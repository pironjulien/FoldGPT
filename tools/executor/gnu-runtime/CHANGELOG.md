# Changelog

## 2026-09-06

- Diagnose Bionic's complete dependency traversal and derive the GNU descriptor
  capacity from its actual immutable APK/system ELF closure plus the existing
  process capacity; preserve the static runner's original limit.
- Verify a real Bash login shell and workspace profile, native descriptor
  ceiling, and null redirection without disabling system startup scripts.
- Add an opt-in managed GNU process backend with actual stdin/output/lifecycle,
  full workspace and separate temporary-root policy decisions, pinned native
  namespace mutations, and no implicit temporary write grant.
- Separate internal PRoot scratch authority from guest temporary paths; protect
  the immutable tracer PID, preserve the real F2FS probe in trusted bootstrap,
  and retain leases after unknown descendant cleanup.
- Add reproducible host source snapshots and eight real integration cases;
  expose explicit Android rootfs/loader/memory/deadline inputs to the same suite.
- Add independent read-only Android collection of project/archive bytes, denial
  evidence, installed APK and reproducible native stripping, current process
  observations and 6,200 official-client files against the historical package.
- Add a native GNU Bash/Python project diagnostic behind actual Landlock and
  seccomp, using the separate strict PRoot candidate and no global read grant.
- Verify source editing, bytecode compilation, three tests, ZIP packaging,
  package execution and nonzero exit propagation on an ordinary Linux UID.
- Record native security denials and independently verify retained project bytes;
  provide standalone NDK inputs without changing the normal client runtime.
