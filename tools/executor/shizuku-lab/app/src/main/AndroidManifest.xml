<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:allowBackup="false" android:label="FoldGPT Shizuku qualification"
        android:supportsRtl="true" android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity android:name=".ProbeActivity" android:exported="true"
            android:permission="android.permission.DUMP"
            android:configChanges="orientation|keyboardHidden|screenSize|smallestScreenSize" />
        <provider android:name=".ProbeProvider"
            android:authorities="${applicationId}.shizuku" android:enabled="true"
            android:exported="true" android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />
    </application>
</manifest>
