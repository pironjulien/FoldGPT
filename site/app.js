const canvas=document.getElementById('particles');const ctx=canvas.getContext('2d');let w,h,dpr,pts=[];const mouse={x:-9999,y:-9999};
function resize(){dpr=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;canvas.width=w*dpr;canvas.height=h*dpr;canvas.style.width=w+'px';canvas.style.height=h+'px';ctx.setTransform(dpr,0,0,dpr,0,0);const n=Math.min(105,Math.max(42,Math.floor(w*h/15000)));pts=Array.from({length:n},()=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.18,vy:(Math.random()-.5)*.18,r:Math.random()*1.5+.25,c:Math.random()<.14?'#f2a65a':Math.random()<.18?'#a889ff':'#70d9df'}))}function frame(){ctx.clearRect(0,0,w,h);for(const p of pts){const dx=mouse.x-p.x,dy=mouse.y-p.y,dist=Math.hypot(dx,dy);if(dist<150){p.vx-=dx/dist*.003;p.vy-=dy/dist*.003}p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>w)p.vx*=-1;if(p.y<0||p.y>h)p.vy*=-1;ctx.beginPath();ctx.fillStyle=p.c;ctx.globalAlpha=.5;ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill()}for(let i=0;i<pts.length;i++)for(let j=i+1;j<pts.length;j++){const a=pts[i],b=pts[j],dist=Math.hypot(a.x-b.x,a.y-b.y);if(dist<105){ctx.beginPath();ctx.strokeStyle='#6f7890';ctx.globalAlpha=(1-dist/105)*.12;ctx.lineWidth=1;ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}}ctx.globalAlpha=1;requestAnimationFrame(frame)}addEventListener('resize',resize);addEventListener('pointermove',e=>{mouse.x=e.clientX;mouse.y=e.clientY});addEventListener('pointerleave',()=>{mouse.x=-9999;mouse.y=-9999});resize();frame();
