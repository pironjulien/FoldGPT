# Pause demandée par Julien le 8 septembre 2026

Le travail est arrêté à sa demande, sans installation de candidat ni nouveaux
essais après sa demande. Dossier unique : `C:\Dev\ChatgptFold`.

## Téléphone au repos

L'APK installé reste **r23/versionCode13**, SHA256
`3a1bb00e73a828fa298884941514ad421a5f888c7fe483303b0ad514f2122c74`.
La session UI 6893 a fermé normalement à 12:31 UTC : deux reçus réels de
nettoyage/wait0, processus/socket/manifeste/marqueur absents. Preuve :
`work/r24-native-20260908/ui/stop-first/report.json` et fichiers associés.
Le projet addition collecté juste avant est inchangé, archive SHA256
`c6a77ffdbf39fc3187c261db6fa2e2b72073859fc53de185dd5222ef2b6284a4`.
Le maintien éveillé USB a été remis à `0`, écran d'accueil puis veille demandés.
Boot observé inchangé : `348d453e-f4e5-40e0-8ef0-030f4d5e38af`.

## Avancées vérifiées de cette séance

- Ripgrep **15.2.0 + PCRE2 10.47/JIT** : deux compilations Android identiques
  dans `work/native-ripgrep-20260908/build-v4`, ELF AArch64/Bionic, linker64,
  alignement 16 Kio, dépendances libc/libdl seulement ; 15 cas d'admission
  passent. Le vrai compiler-rt NDK résout `__clear_cache`. **Aucune exécution
  Android de rg ni JIT encore effectuée.** Sources et commandes dans
  `tools/executor/bionic-runtime/README-ripgrep.md`.
- Stage, admission et package **r24** produits sous
  `downloads/native-ordinary-uid-20260908/production-{stage,admission,package}-r24`.
  89 bibliothèques, 2447 données Python, 87 alias, 95 sources.
  Le numéro source passe à versionCode14. **APK r24 non construit et non installé.**
  Le complément de notices toolchain arrivé ensuite reste à intégrer avant
  distribution ; conserver le package actuel et créer une nouvelle sortie.
- Huit tests d'entrées/alias passent sur l'APK r23 réel ; six tests propres à rg
  sont explicitement ignorés jusqu'au nouvel APK. L'APK r23 reste accepté par
  le vérificateur étendu. Deux tests du staging de qualification rg passent.
- Pip **26.2.1** démarre réellement avec le Python Android installé, UID10412,
  cache hors runtime : `work/r24-native-20260908/pip-preflight-63a2aba2/report.json`.
  Test via ADB, **pas une conversation, ni téléchargement/installation validés**.
- Terminal natif : probe v3 réussie sous UID10412, vrai openpty, terminal de
  contrôle, saisie canonique/echo, octets bruts, resize/SIGWINCH, Ctrl+C saisi,
  ioctl TIOCSIG, sortie enfant23 et nettoyage/wait/ECHILD réels. Preuve :
  `work/r24-native-20260908/pty-probe-c6133fd7/report.json`. Enfant3993 absent,
  boot inchangé. **Une sonde fixe à un enfant ne valide pas le backend PTY ni
  une commande arbitraire ou les descendants.**

## Limite PTY précisément identifiée

V1 puis v2 sont conservées en échec. `tcgetsid/TIOCGSID` retourne EACCES sur
le Fold ; isatty, setsid/TIOCSCTTY, getpgrp/getsid/getpgid/tcgetpgrp réussissent.
V3 conserve le diagnostic refusé et vérifie le contrôle réel via `/dev/tty`
et son groupe. Elle distingue Ctrl+C tapé et l'ioctl de signal forcé ; les deux
ont réussi sur cet essai. Aucun paramètre des protections Android n'a changé.

Le candidat PTY isolé sous `work/native-pty-20260908` **reste non qualifié et
non intégré**. Contrat amont confirmé par l'agent : tty garde stdin ouvert même
si pipeStdin=false ; taille initiale24x80 ; l'interruption RPC vise le groupe
initial, distinct de l'octet Ctrl+C et du groupe au premier plan. Le candidat
doit respecter ces règles avant promotion. PIDFD_SIGNAL_PROCESS_GROUP est une
piste non testée ; ne pas la présenter comme disponible sur le Fold.

## Reprise concrète

1. Revérifier ADB et état fermé ; activer le maintien éveillé uniquement pour
   les essais. Ne pas réutiliser les anciens PID comme état courant.
2. Compléter les notices Rust/NDK depuis
   `work/native-ripgrep-20260908/toolchain-notices/manifest.json` dans un nouveau
   stage/package attesté ; ne pas modifier silencieusement le package r24 figé.
3. Construire l'APK avec `tools/runtime/build-production-candidate.py`, vérifier
   signature, sources/alias, puis les 14 tests `test-native-entrypoints.py` sur
   cet APK réel. Aucune version r24 installée à ce point de pause.
4. Mise à jour sans désinstallation, après HOME ; qualification
   `tools/runtime/qualify-production-device.py --qualification ripgrep
   --verify-restart --apk-sha256 EMPREINTE`, puis régression ordinary-uid.
5. Vraie conversation : prompt
   `work/native-ripgrep-20260908/ui-qualification/prompt.json`. Nouveau sous-dossier
   dependency-inspector, Packaging26.3 téléchargé réellement sur le Fold,
   installation .deps, modules/tests/zipapp et recherches rg. Le wheel PC est
   vérifié mais ne doit pas remplacer une preuve de téléchargement Android.
6. Ensuite seulement raccorder et qualifier le backend PTY ; préserver les
   sources direct v7 et les essais GPU/ancien PTY en attente.

Toutes les nouvelles sorties restent locales dans le projet. La dernière
sauvegarde chiffrée distante restaurée reste **v15** ; cette séance n'a pas
produit v16. Le prochain complément devra inclure rg, le stage/package, les
probes PTY, les diagnostics et les nouveaux helpers/prompts. Les sources
canoniques de cette séance et cette note peuvent être poussées au dépôt privé
sans revendiquer une sauvegarde distante des nouveaux binaires/journaux.

Architecture inchangée : Bash/Python natifs Android/Bionic, interface et
contrôleur GNU sous PRoot ; aucune VM téléphone. ChatGPT officiel intact,
mises à jour normales conservées. Aucun root, flash ou déverrouillage.
